import java.util.Scanner;

public class programad {

	public static void main(String[] args) {
		
		int mesec;
		
		Scanner scan = new Scanner( System.in );
		System.out.println("Mesec? ");

	     mesec = scan.nextInt();
	   
	     switch (mesec =1) { 
	     case 'a':
	    	 mesec = (int) 3.8;
	    	 System.out.print("Lihven br: " + 3.8);
	    	 break;}
	     switch (mesec = 3 ) {
	     case 'b':
	     mesec = (int) 3.5;
	    	 System.out.print("Lihven br: " +3.5);
	    	 break;}
	     
	    
	   
	    System.out.print(mesec);
	}

}
